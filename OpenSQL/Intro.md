1️⃣ SELECT พื้นฐาน (ต้องแม่นก่อน)
2️⃣ SELECT SINGLE vs UP TO 1 ROWS
3️⃣ เลือกเฉพาะ Field (Performance สำคัญ)
4️⃣ WHERE + AND/OR + BETWEEN + LIKE
6️⃣ INNER JOIN (สำคัญมากในงานจริง)
7️⃣ FOR ALL ENTRIES (ABAP Style Bulk Fetch)
8️⃣ GROUP BY + COUNT / SUM